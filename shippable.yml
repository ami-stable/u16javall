language: java

jdk:
  - openjdk8
  - openjdk7 
  - oraclejdk8
  - oraclejdk7
 
build:

  ci:
    - java -version
    - echo $SHIPPABLE_JDK_VERSION
   # - mvn --version    
   # - ant -version 
   # - gradle --version 
   
